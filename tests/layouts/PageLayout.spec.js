import React from 'react'
import PageLayout from 'layouts/PageLayout/PageLayout'
import { shallow } from 'enzyme'

describe('Example for creating tests', () => {
  it('Fake test just for example', () => {
    expect(true).to.equal(true)
  })
})
